{
  "name": "hackathon-2022",
  "packageManager": "yarn@3.2.0",
  "workspaces": [
    "./apps/*"
  ],
  "scripts": {
    "format": "prettier --write '**/*.{ts,tsx,js,css,json,md}'",
    "client:deploy": "vercel . --prod",
    "storage:deploy": "yarn workspace storage publish"
  },
  "dependencies": {
    "typescript": "^4.6.2"
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
